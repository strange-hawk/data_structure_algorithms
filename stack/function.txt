Applications of stack are:
    -   Function calls are implemened using stack
    -   check for balanced parenthesis
    -   Reversing item
    -   Infix to prefix/postfix
    -   evaluation of postfix/prefix
    -   stack spam problem
    -   Next greater problem
    -   undo/redo
    -   forward/backward


    stack can be made on any containers that offer back,size,empty,push_back,pop_back functionality
    stack -> container adapter becaiuse it provides only interface but is actually implemented using other containers